services:
  pc-connector-web:
    build: .
    image: pc-connector-web:latest
    container_name: pc-connector-web
    restart: unless-stopped
    network_mode: host
    environment:
      - PORT=8080

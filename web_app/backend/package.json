{
  "name": "pc-connector-web-backend",
  "version": "1.0.0",
  "description": "Backend-for-frontend for the PC Connector web app: serves the Angular SPA, proxies to the PC agent (bypasses CORS) and sends Wake-on-LAN / discovery packets from the always-on server.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy": "^1.18.1"
  }
}
